{
  "buildCommand": "cd price-tracker-frontend && npm install && npm run build",
  "outputDirectory": "price-tracker-frontend/dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
